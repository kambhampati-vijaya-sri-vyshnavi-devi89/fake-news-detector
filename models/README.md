# Place fine-tuned model files here

## Expected files for production:
- `bert-fake-news/` — Fine-tuned BERT weights (HuggingFace format)
- `roberta-fake-news/` — Fine-tuned RoBERTa weights
- `tfidf_vectorizer.pkl` — Trained TF-IDF pipeline

Download pretrained weights from HuggingFace or train using the notebook in `notebooks/`.
